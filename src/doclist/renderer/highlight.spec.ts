import HighlightRenderer, { Highlight_t } from "./highlight";
import { ColumnRenderer_Config_t, ColumnRenderer_Props_t } from "./interface";

let config: ColumnRenderer_Config_t<Highlight_t[]>;
let props: ColumnRenderer_Props_t;

//let reactHtmlStuff = HighlightRenderer(config)(props);